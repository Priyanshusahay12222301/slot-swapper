# Production Environment Variables for Backend (Render)
# Copy these to your Render service environment settings

NODE_ENV=production
PORT=10000
MONGO_URI=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/slotswapper?retryWrites=true&w=majority
JWT_SECRET=generate-a-64-character-random-string-for-production

# To generate JWT_SECRET, run in terminal:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"